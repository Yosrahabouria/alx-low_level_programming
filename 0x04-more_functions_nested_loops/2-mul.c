#include "main.h"
#include<stdio.h>
/**
 * mul - return result of multiplication
 * Return:always 0 (success)
 * @a : the integer
 * @b : the integer
*/
int mul(int a, int b)
{
int n;
n = a * b;
return (n);
}
