#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strlen- return length of string
 * Return:always 0 (success)
 * @s : the input string
*/
int _strlen(char *s)

{
	int len;

	len = strlen(s);
	return (len);

}
