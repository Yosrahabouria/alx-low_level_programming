#include "main.h"
/**
 * _islower - print alphabet in lower case
 * Return:always 0 or 1 (success)
*/
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
{
else return (0);
}
_putchar('\n');
}
