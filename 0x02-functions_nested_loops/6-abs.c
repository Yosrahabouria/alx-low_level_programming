#include "main.h"
/**
 * _isalpha - check if alphabet uppercase or lower
 * Return:always 0 (success)
 * @c : the character ASCII code
*/
int _abs(int)
{
	if (a >= 0)
{ 
		return (i);
}
	else 
	{
		return (i * -1);
	}
_putchar('\n');
}
